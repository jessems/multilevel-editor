
## [2026-09-24] fix | multilevel-editor: insert-between hint swallowed drop-above drags — the fixed-position hint strip sat over the drop-before zone and, with no mousemove firing during native drags, never hid, hit-testing outside #tree and eating dragover; fixed by hiding the hint on dragstart (upstream commit, v0.2.1); reproduced and verified with Playwright (hint hides at dragstart, drop zone hit-tests into tree, full drag-above moves the bullet); live server relaunched via uvx

## [2026-09-24] tooling | multilevel-editor: Cmd/Ctrl+S triggers Save (doSave() extracted; shortcut preventDefaults the browser dialog, saves when dirty, flashes "no changes to save" when clean; works from tree, markdown view, and mid-edit) — v0.2.2 pushed; verified via Playwright round trip (clean refuse + dirty save landing in the file); live server relaunched via uvx

## [2026-09-25] fix | multilevel-editor: settings sidebar toggle is now a gear icon (feather settings SVG mask, consistent with the action-icon style) at the far right of the header, replacing the hamburger glyph — v0.5.1 pushed; verified statically (SVG well-formed, JS parses; Playwright browser backend crashed mid-session)

## [2026-09-25] fix | multilevel-editor: settings cog wrapped to the left with long titles (header flex-wrap carried the last flow item onto a new row) — now absolutely pinned to the sticky header top-right corner with reserved padding; v0.5.2 pushed; verified by headless-shell screenshots at 700px (four-row wrapped header, cog top right) and 1400px; playwright MCP browser was dead so verification went through chrome-headless-shell --screenshot directly; live server relaunched

## [2026-09-25] fix | multilevel-editor: insert-between hint now stays available while a bullet is in edit mode (the hover handler used to hide it whenever a contenteditable had focus) — clicking the plus blurs/stages the open edit first, then opens the new bullet; a mousedown snapshot of the target's neighbours covers the case where the open edit was an empty pending bullet removed on blur (possibly the hint's own target). v0.5.3 pushed; verified with Playwright (node, headless-shell 1208): hint shows mid-edit, plus inserts at the hovered boundary with the prior edit staged, empty-pending top/bottom-edge cases reopen a fresh bullet, no page errors

## [2026-09-25] fix | multilevel-editor: drags released beside a row (indent gutter, page margin) at the right height did nothing — dragover/drop only accepted a pointer directly over a .row. Drop target is now resolved by vertical position (row under the pointer, else nearest visible row within 40px), handlers moved to document so margins outside #tree count, stale drop marks cleared when no valid target. Also the insert-between hint is suppressed while a mouse button is held (e.buttons), so a text-press drag no longer shows a plus that a release cannot honour. v0.5.4 pushed; verified with Playwright: gutter drop before a row, right-margin drop after a row, text-press shows no hint and moves nothing, far-away drag is a no-op, plain hover still shows the hint

## [2026-09-25] tooling | multilevel-editor: level switch in the header — one mode per depth the outline uses (1 = top level only … N = all levels), replacing Expand-all/Collapse-all; buttons rebuilt on every structural change (incl. markdown-view rebuilds), active mode highlighted by matching the open/closed state (cleared on manual caret toggle), chosen level remembered per file across the post-save reload, bar hidden in markdown view and for flat lists. v0.6.0 pushed; verified with Playwright (headless-shell 1208): modes 1/2/3 show 1/5/12 rows, caret toggle deselects, reload restores level 2, 4th level via markdown adds a 4th button, no page errors

## [2026-09-25] fix | multilevel-editor: level switch now HIDES the deeper levels instead of collapsing them — mode N is a depth filter (generated stylesheet removes the sub-lists below level N, cut-level carets render as leaf dots and are inert), independent of caret state; picking N opens every shallower branch; "all" is remembered as all so a newly added deeper level stays visible; Enter on a cut-level bullet adds a sibling rather than a hidden child, Tab into a hidden level is refused with a notice. v0.6.1 pushed; verified with Playwright (headless-shell 1208): modes 1/2/3 show 1/5/12 rows with deepest visible depth 1/2/3, cut caret is a dot with pointer-events none, top-level caret toggle keeps the mode, Enter yields a visible depth-2 sibling, Tab flashes the notice, reload restores level 2, 4th level under "all" stays visible; no page errors

## [2026-09-25] fix | multilevel-editor: level switch moved to its own row centred at the bottom of the header (row extends 32px into the cog padding so its centre is the page centre; hidden when empty or in markdown view; symmetric at the mobile breakpoint). v0.6.2 pushed; verified with Playwright at 1200px and 700px: switch centre = viewport centre, below every other header row, hidden in markdown mode

## [2026-09-25] fix | multilevel-editor: level-1 mode dropped when the top level is a single bullet (a lone title shows nothing useful) — the switch starts at 2; a stored level below the minimum is clamped; outlines with several top-level bullets keep mode 1. v0.6.3 pushed; verified with Playwright: single-title demo offers 2/3 with a stale stored 1 clamped to 2, adding a second top-level bullet via markdown brings mode 1 back

## [2026-09-25] tooling | multilevel-editor: level switch re-based on HEADING levels — a bullet's level is its # count, every non-heading bullet is the paragraph level (deepest heading + 1) wherever it nests, so the highest mode is the paragraph level and heading views never mix in paragraphs (no-heading outlines fall back to depth); hiding via per-li classes instead of a generated stylesheet, branches with all children hidden get the dot, edited bullets always visible (a paragraph staged in a heading view hides with a notice), depth-based Enter/Tab guards dropped. v0.7.0 pushed; verified with Playwright on a copy of the SVIT skeleton: modes 2/3/4 show {h1,7×h2} / {+7×h3} / {+82 paragraphs}, new bullet visible while editing, staged ### stays, staged paragraph hides with notice, reload restores level 3; no page errors

## [2026-09-26] tooling | multilevel-editor: 5th level — fully written paragraphs. A non-heading bullet nested under a paragraph is that paragraph's written text (level = paragraph level + 1; the mode is offered whenever the outline has paragraphs): no grip/dot/number, moves only with its paragraph (dragstart refused, drops never land inside a paragraph), nothing nests under it, the paragraph above renders semibold; created via Tab under the paragraph above or in the markdown view; several per paragraph allowed (lettered sub-items). Nesting rules relaxed accordingly (old "nothing nests under a paragraph" cap replaced). v0.8.0 pushed; verified with Playwright on a skeleton copy with two written paragraphs: buttons 2–5, level 4 hides the text and dots its paragraphs, level 5 shows it with no grip/chip, Tab makes a paragraph the written text of the one above and Shift+Tab undoes it, Tab under written text refused, a bullet with children refused as written text; no page errors

## [2026-09-26] tooling | multilevel-editor: quill button on every numbered paragraph (next to the trash, on hover) writes the paragraph — POST /generate sends the staged outline with the target marked; the server prompts a model for one paragraph confined to the outline's content (keeps [cite] placeholders, no invented facts) and the reply is inserted as the paragraph's written text (level 5), staged, undoable (⌘Z), saved on Save, view jumps to level 5; earlier written text kept below. Backends: `anthropic` SDK when installed with ANTHROPIC_API_KEY, else the `claude` CLI on PATH run tool-less/non-interactively in the outline dir (CLAUDECODE unset so it works inside a session); --model (default claude-opus-5). v0.9.0 pushed; verified with Playwright on a skeleton copy: 81 quills on 81 numbered rows, none elsewhere; real hover+click generation (~8 s via claude CLI) inserted a written paragraph under ¶3 with placeholders kept, level switched to 5, Save armed, ⌘Z removed it; no page errors

## [2026-09-26] convention | multilevel-editor: written paragraphs are a DRAFT stored beside the skeleton, never in it — on Save the structure (levels 1–4) goes to the skeleton .md and the whole outline + written text (level 5) to `<skeleton>.draft.md` (frontmatter summary/source/status: draft); on load the text is re-attached by topic-sentence fingerprint; text whose paragraph vanished is reported and kept at the end of the draft under "# Orphaned draft text" (paragraph + text), round-tripped via the page on save; conflict hash now spans skeleton + tags + draft. reverse-outline skill: sub-items are folded into the paragraph bullet (level 5 reserved for draft text). v0.10.0 pushed; verified with Playwright on a skeleton copy: skeleton byte-identical after saving two written paragraphs, draft = 97 bullets + 2 written with frontmatter, reload re-attaches, external edit of a topic sentence → orphan notice and orphan section in the draft, preserved across saves; no page errors

## [2026-09-26] fix | multilevel-editor: a numbered paragraph that gained written text acquired a caret that pushed its heading 28px right of its sibling paragraphs, and the written text started left of the heading — numbered rows now hide the caret as they hide the dot (the level switch shows/hides the text), and the written-text marker slot is 40px so its prose starts just past the heading text for any chip width. v0.10.1 pushed; measured with Playwright: written heading text x = sibling text x, body text 9px right of it

## [2026-09-26] tooling | multilevel-editor: level-switch buttons carry icons instead of numbers — heading/depth modes show a bulleted staircase of n dot-and-bar rows (equal-length bars, so it cannot be mistaken for right-aligned text; capped at 6), the paragraph mode a pilcrow, the written-text mode lines of prose; inline currentColor SVG so the active (accent) state inverts cleanly, tooltip doubles as aria-label; buttons rebuilt on a min-max-H signature so a change in heading depth re-icons them. v0.10.2; verified by headless-shell screenshots of the demo (modes 2–5) and a two-title copy (modes 1–5)

## [2026-09-26] fix | multilevel-editor: typography pass for the paragraph hierarchy — sections/counts/paragraphs get a vertical rhythm (36/22/8px), ### counts upright semibold instead of italic, a written paragraph's topic sentence is a semibold run-in heading over its prose (no tree line under it, prose 17.5px/1.72, max 62ch, 14px after), inline [cite]/[fact: …] placeholders render as quiet small muted sans (no chip background in prose), whole-bracket bullets muted italic. v0.11.0 pushed (on top of the concurrent icon-button change 9770a0d from another session); checked by screenshots at levels 4 and 5 in light and dark

## [2026-09-26] fix | multilevel-editor: numbered paragraphs form one column regardless of heading depth — a paragraph hanging directly under a ## (or under the title) is shifted right to the depth a paragraph under the deepest heading level occupies (li.para with --shift = missing levels × (indent + 1px list border); indent is a --indent variable, 26px / 18px mobile); written text follows its paragraph. v0.11.1 pushed; measured with Playwright on a demo with a ### sub-heading: all 8 paragraph texts at x=307, headings still stepped (## 219, ### 246)

## [2026-09-26] tooling | multilevel-editor: Navigator — left sidebar listing the outline as a tree that descends with the level switch (mode N: nodes above N expanded, level-N nodes collapsed; paragraphs under a ## stay out of heading views; written text never listed); manual caret expands show all children until the next level change; clicking a label scrolls to the bullet under the sticky header and flashes it, switching to its level first when hidden; panel-icon toggle top left, remembered on wide screens, overlay under 900px; the sidebar is not a drop margin for drags. v0.12.0; verified with Playwright (headless-shell 1208) on a copy of the SVIT skeleton: modes 2/3/4/5 list 8/15/97/97 nodes, caret expand reveals a section's 14 paragraphs, jump to a hidden paragraph switches to level 4 and lands 12px under the header, text column clears the sidebar (310 > 264), closed state survives reload, narrow viewport starts closed and closes after a jump; no page errors

## [2026-09-26] tooling | multilevel-editor (branch feat-skeleton-generation, worktree multilevel-editor-skeleton-generation): skeleton variants — "Skeleton variant…" button top right of the text opens a dialog (instruction box with the example "rebuild in a different legal argumentation style", Use default settings runs the example, Generate runs the typed instruction); POST /variant has the model rewrite the skeleton on disk (same facts/placeholders/language; reply validated as an outline starting with a heading, indentation normalised) and writes <skeleton>.variant-N.md beside it with frontmatter (summary, variant_of, variant, title, prompt, created, status: draft), then opens it via ?doc=<name>. Variants are full skeletons with their own .draft.md/.tags.yaml (drafts stay one file per variant). Server started on the base serves the family only (other names → 404); save/generate/variant carry the doc; navigator top level = file family (open one expands onto its outline, others are links). v0.13.0 committed and pushed on the branch, not merged; verified with Playwright on a demo copy: real generation in 17 s produced demo.variant-1.md (14 bullets, 6 headings, frontmatter), page opened it with nav "demo / Variant 1*", save on the variant changed only the variant and wrote its own draft, base unchanged, nav link returns to the base; bogus ?doc= → 404
